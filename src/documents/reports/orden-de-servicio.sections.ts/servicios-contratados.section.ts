import { Content } from "pdfmake/interfaces"


interface InfoOrdenOptions {
    folio
}

export const serviciosContratadosOrdenSection = async (info:InfoOrdenOptions):Promise<Content> => {
    return
}